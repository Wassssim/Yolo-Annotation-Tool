import React, { lazy, Suspense } from 'react';

/*const LazyAnnotationCanvas = lazy(() => import('./AnnotationCanvas'));

const AnnotationCanvas = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
  <Suspense fallback={null}>
    <LazyAnnotationCanvas {...props} />
  </Suspense>
);

export default AnnotationCanvas;*/
