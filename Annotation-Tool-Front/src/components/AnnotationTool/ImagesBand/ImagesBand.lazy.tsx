import React, { lazy, Suspense } from 'react';

/* const LazyImagesBand = lazy(() => import('./ImagesBand'));

const ImagesBand = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
  <Suspense fallback={null}>
    <LazyImagesBand {...props} />
  </Suspense>
);

export default ImagesBand;
 */
