import React from 'react';
/*import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import ImagesBand from './ImagesBand';

describe('<ImagesBand />', () => {
  test('it should mount', () => {
    render(<ImagesBand />);
    
    const imagesBand = screen.getByTestId('ImagesBand');

    expect(imagesBand).toBeInTheDocument();
  });
});*/