import React, { lazy, Suspense } from 'react';

/*const LazyImageWithLoader = lazy(() => import('./ImageWithLoader'));

const ImageWithLoader = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
  <Suspense fallback={null}>
    <LazyImageWithLoader {...props} />
  </Suspense>
);

export default ImageWithLoader;*/
